name: bgcflow
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - openjdk
  - singularity
  - pip
  - pip:
    - bgcflow_wrapper
    - pre-commit
    - pytest-cov
    - alive-progress
